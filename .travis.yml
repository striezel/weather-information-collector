language: cpp
compiler:
  - clang
  - gcc
sudo: false
dist: trusty
os:
  - linux
script:
  - ./ci/cpplint.sh
  - mkdir ./build
  - cd ./build
  - cmake ../
  - make -j4
  - echo "Test still need to be implemented..."
#  - ctest -V
addons:
  apt:
    packages:
    - g++
    - libcurl4-gnutls-dev
    - libjsoncpp-dev
notifications:
  email: false
